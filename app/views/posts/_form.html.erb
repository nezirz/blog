<dev>
	<%= form_for @post do |f| %>

 <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@post.errors.count, "error") %> prohibited
        this post from being saved:
      </h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

	
  	<div><%= f.label :title %><br>
    	 <%= f.text_field :title ,:size =>"70x10",:class =>"form-control"%><br>
    	 <%= f.label :body %><br>
        <%= f.text_area :body ,:size =>"70x10",:class =>"form-control"%><br>
      <div class="col-md-12">
          <div class="col-md-6">
            <%= link_to "Cancel", :back  ,:class =>"form-control"%> 
          </div>
          <div class="col-md-6">
            <%= f.submit "Create Post",:class =>"form-control" %>
          </div>
      </div>
      <br>

    </div>
	<% end %>
</dev>